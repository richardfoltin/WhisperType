# CODEMAP

Generated by spec-hub codemap at commit dca0c85 — do not edit.
Coverage is total: every directory appears, as its own `##` section
or inside a parent's `dir/ (N files)` line; the token budget only
sets the level of detail. `imported by N` = N other files import
that file; `used in N` = N other files import that exported name.

## . (repo root)

- setup_mac.py:
  - `APP = ["main.py"]`
  - `OPTIONS = …`
- files: CLAUDE.md, LICENSE, README.md, SPEC.md, config.template.json, initial_prompt.md, install.bat, install_mac.sh, main.py, requirements-mac.txt, requirements.txt, start.bat, start_debug.bat, start_silent.vbs, uninstall_mac.sh, whispertype.pyw

## whispertype

- log.py — imported by 9 files:
  - `LOG_PATH = …` (used in 2 files)
  - `MAX_BYTES = 1_000_000`
  - `def init(redirect_std=True):`
  - `def log(msg):` (used in 9 files)
  - `def close():`
- __init__.py — imported by 5 files:
  - `def main():` (used in 2 files)
- config.py — imported by 3 files:
  - `CONFIG_DIR = Path.home() / ".whispertype"`
  - `CONFIG_PATH = CONFIG_DIR / "config.json"`
  - `API_KEY_PATH = CONFIG_DIR / "openai_api_key"` (used in 3 files)
  - `IS_MAC = sys.platform == "darwin"`
  - `DEFAULTS = …`
  - `class Config:`
  - `def load():`
- jobs.py — imported by 3 files:
  - `MAX_HISTORY = 50`
  - `HISTORY_PATH = Path.home() / ".whispertype" / "history.json"`
  - `class JobStatus(Enum):` (used in 3 files)
  - `@dataclass class ModelSwitch:` (used in 1 file)
  - `@dataclass class EngineSwitch:` (used in 1 file)
  - `@dataclass class ModelUnload:` (used in 1 file)
  - `@dataclass class TranscriptionJob:` (used in 1 file)
  - `@dataclass class BenchmarkResult:` (used in 1 file)
  - `@dataclass class BenchmarkJob:` (used in 1 file)
  - `class JobQueue:` (used in 1 file)
- icon.py — imported by 2 files:
  - `@lru_cache(maxsize=None) def make_tray_icon(state="idle"):` (used in 1 file)
  - `@lru_cache(maxsize=None) def png_bytes(state="idle"):` (used in 1 file)
- transcribe.py — imported by 1 file:
  - `IS_MAC = sys.platform == "darwin"`
  - `@dataclass class ModelInfo:`
  - `def pcm_to_float32(audio_bytes):`
  - `DECODE_OPTIONS = …`
  - `PROMPT_PATH = …`
  - `PROMPT_TOKEN_BUDGET = 223`
  - `def initial_prompt():`
  - `def report_prompt_budget(language):`
  - `class WhisperEngine:`
  - `class MlxEngine:`
  - `OPENAI_FALLBACK_MODELS = …`
  - `class OpenAiEngine:`
  - … +1 more exports
- app.py:
  - `DOUBLE_TAP_MS = 400`
  - `GPU_WINDOW = 60`
  - `KEY_MAP = …`
  - `KEY_LABEL = …`
  - `class App:`
- audio.py:
  - `IS_MAC = sys.platform == "darwin"`
  - `def backend():`
  - `def resolve_device(spec):`
  - `class Capture:`
  - `def warm_up(cfg):`
  - `def record_until_stop(cfg, stop_event, level_callback=None, on_first_chunk=None):`
- singleton.py:
  - `IS_WINDOWS = sys.platform == "win32"`
  - `def claim():`
  - `def notify_already_running():`

## whispertype/backends

- base.py — imported by 2 files:
  - `class Backend:` (used in 2 files)
- mac_gpu.py — imported by 1 file:
  - `class AppleGPU:` (used in 1 file)
- __init__.py:
  - `def create_backend():`
- macos.py:
  - `kVK_Return = 0x24`
  - `kVK_Tab = 0x30`
  - `EVENT_MARKER = 0x57485459`
  - `class MacTarget:`
  - `class MacBackend(Backend):`
  - `def secure_input_enabled():`
- windows.py:
  - `INPUT_KEYBOARD = 1`
  - `KEYEVENTF_UNICODE = 0x0004`
  - `KEYEVENTF_KEYUP = 0x0002`
  - `VK_MENU = 0x12`
  - `VK_RETURN = 0x0D`
  - `class MOUSEINPUT(ctypes.Structure):`
  - `class KEYBDINPUT(ctypes.Structure):`
  - `class HARDWAREINPUT(ctypes.Structure):`
  - `class INPUT(ctypes.Structure):`
  - `TYPE_CHUNK_CHARS = 200`
  - `class WindowsBackend(Backend):`

## whispertype/ui

- common.py — imported by 2 files:
  - `LANGUAGES = …` (used in 2 files)
  - `IDLE_CHOICES = …` (used in 2 files)
  - `ENGINES = …` (used in 1 file)
- __init__.py:
  - `def create_ui_factory():`
- appkit_ui.py:
  - `OV_W = 380`
  - `OV_H_COMPACT = 212`
  - `OV_H_QUEUE = 300`
  - `OV_H_HISTORY = 470`
  - `HTML_PATH = Path(__file__).with_name("overlay.html")`
  - `class OverlayPanel(NSPanel):`
  - `class OverlayWebView(WKWebView):`
  - `class Bridge(NSObject):`
  - `class AppKitUI:`
- tk_ui.py:
  - `OFF_SCREEN = "-9999+-9999"`
  - `OV_W = 380`
  - `MAX_QUEUE_VISIBLE = 5`
  - `VISIBLE_HISTORY = 8`
  - `HISTORY_ITEM_H = 22`
  - `class TkUI:`
- files: overlay.html
_(1 of 93 exported symbols not detailed — every file is still named above or counted in a `dir/` line at commit dca0c85)_
